package main

import "core:fmt"

Map :: struct {
    data: [dynamic]int,
}

hash :: proc(n: int) -> int {
    return (n * 10 + 2) / 3
}

map_init :: proc() -> Map {
    hashmap := Map{}
    hashmap.data = make([dynamic]int, 0, 32)
    return hashmap
}

map_insert :: proc(hashmap: Map, key: int) {
    index := hash(key) % cap(hashmap.data) 
}

map_delete :: proc() {

}

main :: proc() {
    hashmap := map_init()
    fmt.println("Hello, world!")
}



